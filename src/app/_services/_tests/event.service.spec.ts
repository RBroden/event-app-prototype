import { EventService } from '../event.service';

describe('service without testbed', () => {

    let service: EventService;

    beforeEach(() => {
        //service = new EventService();
    });

    /*
    it ('getData should return "data"', () => {
        expect(service.getData()).toBe('data');
    });

    it ('setData should assign "newData" to data propery', () => {
        service.setData('newData');
        expect(service.getData()).toBe('newData');
    });
    */

});
